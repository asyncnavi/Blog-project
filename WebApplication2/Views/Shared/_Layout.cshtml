<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"] - WebApplication2</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>
    <link rel="stylesheet" href="~/WebApplication2.styles.css" asp-append-version="true"/>
</head>
<body>

<header class="text-gray-600 body-font sticky-top top-0">
    <div class="container mx-auto flex flex-wrap flex-col py-4 md:flex-row items-center justify-center">
        <a class="flex title-font font-medium items-center text-gray-900 mb-4 md:mb-0" asp-area="" asp-controller="Home" asp-action="Index">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-10 h-10 text-white p-2 bg-red-500 rounded-full" viewBox="0 0 24 24">
                <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path>
            </svg>
            <span class="ml-3 text-xl">AsyncArmy</span>
        </a>
        <nav class="md:ml-auto md:mr-auto flex flex-wrap items-center text-base justify-center">
            <a class="mr-5 hover:text-gray-900 @(ViewContext.RouteData.Values["controller"]?.ToString() == "Home" && ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "bg-red-500 text-white" : "")"
               asp-area="" asp-controller="Home" asp-action="Index">Feed</a>

            <a class="mr-5 hover:text-gray-900 @(ViewContext.RouteData.Values["controller"]?.ToString() == "Saved" && ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "bg-red-500 text-white" : "")"
               asp-area="" asp-controller="Saved" asp-action="Index">Saved</a>

            <a class="mr-5 hover:text-gray-900 @(ViewContext.RouteData.Values["controller"]?.ToString() == "Join" && ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "bg-red-500 text-white" : "")"
               asp-area="" asp-controller="Join" asp-action="Index">Join Community</a>

            <a class="mr-5 hover:text-gray-900 @(ViewContext.RouteData.Values["controller"]?.ToString() == "About" && ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "bg-red-500 text-white" : "")"
               asp-area="" asp-controller="About" asp-action="Index">About Us</a>
        </nav>
        <button class="inline-flex items-center bg-gray-100 border-0 py-1 px-3 focus:outline-none hover:bg-gray-200 rounded text-base mt-4 md:mt-0">Login
            <svg fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-4 h-4 ml-1" viewBox="0 0 24 24">
                <path d="M5 12h14M12 5l7 7-7 7"></path>
            </svg>
        </button>
    </div>
</header>
<div class="container mx-auto">
    <main role="main" >
        @RenderBody()
    </main>
</div>

<footer class="border-top footer text-muted">
  <!-- Footer will be here... -->
</footer>
<script src="~/js/site.js" asp-append-version="true"></script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>